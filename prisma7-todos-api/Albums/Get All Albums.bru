meta {
  name: Get All Albums
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/albums?page=1&limit=10
  body: none
  auth: none
}

params:query {
  page: 1
  limit: 10
  ~userId: 1
  ~search: album
}

docs {
  Get all albums with pagination and filtering.
  
  Query Parameters:
  - page: Page number (default: 1)
  - limit: Items per page (default: 10)
  - userId: Filter by user ID
  - search: Search by title
  
  Response includes paginated album data with metadata.
}
