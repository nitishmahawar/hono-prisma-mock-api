meta {
  name: Delete Photo
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/photos/{{photoId}}
  body: none
  auth: none
}

docs {
  Delete a photo by ID.
  
  Returns a success message with 200 status code.
}
